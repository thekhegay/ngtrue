dist/
coverage/
docs/.vitepress/dist/
docs/.vitepress/cache/

# Generated by `npm run docs:api`. Formatting it here would make the committed
# copy differ from what the generator emits, and `docs:check` compares the two
# byte for byte.
docs/api/
.fixtures/
